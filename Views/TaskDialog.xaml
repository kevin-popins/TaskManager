<Window x:Class="TaskManager.Views.TaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cv="clr-namespace:TaskManager.Converters"
        Title="{Binding DialogTitle}"
        Width="460" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F8F9FA">

    <Window.Resources>
        <cv:PriorityToDisplayConverter x:Key="PrioDisplay"/>
        <cv:StringToVisibilityConverter x:Key="StrVis"/>

        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#343A40"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="Field" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="28">
        <StackPanel>

            <!-- Title -->
            <TextBlock Text="Название *" Style="{StaticResource Label}"/>
            <TextBox Style="{StaticResource Field}"
                     Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Validation error -->
            <TextBlock Text="{Binding ValidationError}" Foreground="#DC3545" FontSize="12"
                       Margin="0,-10,0,10"
                       Visibility="{Binding ValidationError, Converter={StaticResource StrVis}}"/>

            <!-- Description -->
            <TextBlock Text="Описание" Style="{StaticResource Label}"/>
            <TextBox Style="{StaticResource Field}"
                     Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="72"/>

            <!-- Priority -->
            <TextBlock Text="Приоритет" Style="{StaticResource Label}"/>
            <ComboBox ItemsSource="{Binding Priorities}"
                      SelectedItem="{Binding Priority}"
                      Margin="0,0,0,14" Padding="10,8" FontSize="13">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource PrioDisplay}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <!-- Due Date -->
            <TextBlock Text="Срок выполнения" Style="{StaticResource Label}"/>
            <DatePicker SelectedDate="{Binding DueDate}" Margin="0,0,0,24" FontSize="13"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Style="{StaticResource Btn}"
                        Background="#6C757D" Margin="0,0,10,0"
                        Command="{Binding CancelCommand}"/>
                <Button Content="Сохранить" Style="{StaticResource Btn}"
                        Background="#4361EE"
                        Command="{Binding ConfirmCommand}"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
